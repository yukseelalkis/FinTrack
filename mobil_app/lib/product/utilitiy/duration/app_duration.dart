class AppDuration {
  const AppDuration._(); // private constructor

  static const Duration splashDuration = Duration(seconds: 5);
  static const Duration loginDuration = Duration(milliseconds: 1000);
  static const Duration loginWelcomeDuration = Duration(milliseconds: 1300);
  static const Duration loginTextField = Duration(milliseconds: 1400);
  static const Duration payDuration = Duration(milliseconds: 500);
}

//const Duration(milliseconds: 1400),